fn main() {
    /* Ciclo while*/

    //la estructura de while es la siguiente
    //while condicion {}
    let mut numero=0;

    // aunque, recordemos que while se usa normalmente para ciclos donde el numero de interaciones no se conoce 
    while numero<=10 {
        println!("{}",numero );
        numero+=1
    }
}
